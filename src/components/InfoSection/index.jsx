import React from 'react';
import {Main} from './styles';

const InfoSection = ({children}) => {

    return(
        <Main>
            {children}
        </Main>
    )
}

export default InfoSection;