import React from 'react';
import {Main} from './styles';

const StockSubTitle = ({children}) => {

    return(
        <Main>
            {children}
        </Main>
    )
}

export default StockSubTitle;
